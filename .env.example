# ==============================================================
# Template de variáveis de ambiente
# Copie para .env (local) e preencha com valores reais.
# NUNCA commitar .env — está no .gitignore.
# ==============================================================

# --------------------------------------------------------------
# Cloudflare — autenticação CLI (deploy/migrations)
# --------------------------------------------------------------
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# --------------------------------------------------------------
# Aplicação — secrets de runtime (via wrangler secret put)
# --------------------------------------------------------------
SESSION_SECRET=
JWT_SECRET=

# --------------------------------------------------------------
# AI Gateway
# --------------------------------------------------------------
# Formato: https://gateway.ai.cloudflare.com/v1/<ACCOUNT_ID>/<GATEWAY_NAME>/<PROVIDER>
AI_GATEWAY_URL=https://gateway.ai.cloudflare.com/v1/REPLACE_ACCOUNT/REPLACE_GATEWAY/openai
OPENAI_API_KEY=

# --------------------------------------------------------------
# Runtime
# --------------------------------------------------------------
ENVIRONMENT=development
LOG_LEVEL=debug

# --------------------------------------------------------------
# Testes (opcional)
# --------------------------------------------------------------
# Para rodar e2e contra ambiente remoto em vez do webServer local:
# PLAYWRIGHT_BASE_URL=https://myapp-staging.pages.dev

# --------------------------------------------------------------
# Smoke tests (opcional)
# --------------------------------------------------------------
# SMOKE_BASE_URL=https://myapp-staging.pages.dev
